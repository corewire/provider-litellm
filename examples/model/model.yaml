# After running `make generate`, the litellm_model resource becomes available
# as a Crossplane managed resource:
#
#   apiVersion: litellm.crossplane.io/v1alpha1
#   kind: Model
#   metadata:
#     name: gpt4-proxy
#   spec:
#     forProvider:
#       modelName: gpt-4-proxy
#       customLlmProvider: openai
#       modelApiBase: https://api.openai.com/v1
#       tier: paid
#       mode: chat
#     providerConfigRef:
#       name: default
#
# This example is a placeholder. Run `make generate` to generate the
# actual CRD types and use the correct apiVersion/kind for the Model resource.
#
# Refer to the LiteLLM Terraform provider docs for all available fields:
# https://registry.terraform.io/providers/BerriAI/litellm/latest/docs/resources/model
