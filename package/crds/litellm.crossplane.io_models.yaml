---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: models.litellm.crossplane.io
spec:
  group: litellm.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - litellm
    kind: Model
    listKind: ModelList
    plural: models
    singular: model
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Model is the Schema for the Models API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec defines the desired state of Model
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  additionalLitellmParams:
                    additionalProperties:
                      type: string
                    description: |-
                      map(string). A map of arbitrary additional parameters that will be merged into the litellm_params object sent to the LiteLLM API. This is intended for provider-specific or experimental options not exposed as dedicated arguments.
                      Additional parameters to pass to litellm_params beyond the standard ones
                    type: object
                    x-kubernetes-map-type: granular
                  apiVersion:
                    description: string. The API version to use for the model provider.
                    type: string
                  awsAccessKeyIdSecretRef:
                    description: string (Sensitive). AWS access key ID for AWS-based
                      models.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsRegionName:
                    description: string. AWS region name for AWS-based models.
                    type: string
                  awsRoleNameSecretRef:
                    description: string (Sensitive). AWS IAM role name for cross-account
                      access scenarios.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsSecretAccessKeySecretRef:
                    description: string (Sensitive). AWS secret access key for AWS-based
                      models. As with model_api_key, the value is stored in plaintext
                      in the state file; prefer a litellm_credential referenced via
                      litellm_credential_name and secure your state backend.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsSessionNameSecretRef:
                    description: string (Sensitive). AWS session name for cross-account
                      access scenarios.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  baseModel:
                    description: string. The actual model identifier from the provider
                      (e.g., "gpt-4", "claude-2").
                    type: string
                  customLlmProvider:
                    description: string. The LLM provider for this model (e.g., "openai",
                      "anthropic", "azure", "bedrock").
                    type: string
                  inputCostPerMillionTokens:
                    description: float. Cost per million input tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  inputCostPerPixel:
                    description: float. Cost applied per input pixel for models that
                      charge by image size.
                    type: number
                  inputCostPerSecond:
                    description: float. Cost applied per input second for audio/transcription
                      models.
                    type: number
                  litellmCredentialName:
                    description: |-
                      string. Name of a LiteLLM credential to use for this model.
                      Name of the LiteLLM credential to use
                    type: string
                  mergeReasoningContentInChoices:
                    description: boolean. When set to true, merges reasoning content
                      into the model's choices.
                    type: boolean
                  mode:
                    description: 'string. The intended use of the model. Valid values
                      are:'
                    type: string
                  modelApiBase:
                    description: string. The base URL for the model provider's API.
                    type: string
                  modelApiKeySecretRef:
                    description: string (Sensitive). The API key for the underlying
                      model provider.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  modelName:
                    description: string. The name of the model configuration used
                      to identify the model in API calls.
                    type: string
                  outputCostPerMillionTokens:
                    description: float. Cost per million output tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  outputCostPerPixel:
                    description: float. Cost applied per output pixel for image-generation
                      models.
                    type: number
                  outputCostPerSecond:
                    description: float. Cost applied per output second for audio/transcription
                      models.
                    type: number
                  pricingBaseModel:
                    description: 'string. A pricing key fed to model_info.base_model
                      independently of routing. When set, litellm_params.model still
                      routes via base_model, but LiteLLM looks up cost against this
                      key. Useful when the routing/deployment name differs from the
                      cost-map key — e.g. an Azure deployment routed as azure/gpt-4.1
                      whose real tier is Data Zone: set pricing_base_model = "us/gpt-4.1-2025-04-14"
                      so it is billed at the Data Zone rate. When unset, base_model
                      drives pricing as before.'
                    type: string
                  reasoningEffort:
                    description: 'string. Configures the model''s reasoning effort
                      level. Valid values are:'
                    type: string
                  rpm:
                    description: integer. Requests per minute limit for this model.
                    type: number
                  teamId:
                    description: string. Associate the model with a specific team.
                    type: string
                  thinkingBudgetTokens:
                    description: 'integer. Sets the token budget for the model''s
                      thinking capability. Default: 1024. Note: this field is only
                      relevant when thinking_enabled = true.'
                    type: number
                  thinkingEnabled:
                    description: 'boolean. Enables the model''s thinking capability.
                      Default: false.'
                    type: boolean
                  tier:
                    description: 'string. The usage tier for this model. Valid values
                      are "free" or "paid". Default: "free".'
                    type: string
                  tpm:
                    description: integer. Tokens per minute limit for this model.
                    type: number
                  vertexCredentialsSecretRef:
                    description: string. Vertex credentials (JSON string or path depending
                      on your setup).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  vertexLocationSecretRef:
                    description: string. Vertex AI location (e.g., us-central1).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  vertexProjectSecretRef:
                    description: string. Vertex AI project id (for custom_llm_provider
                      = "vertex").
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  additionalLitellmParams:
                    additionalProperties:
                      type: string
                    description: |-
                      map(string). A map of arbitrary additional parameters that will be merged into the litellm_params object sent to the LiteLLM API. This is intended for provider-specific or experimental options not exposed as dedicated arguments.
                      Additional parameters to pass to litellm_params beyond the standard ones
                    type: object
                    x-kubernetes-map-type: granular
                  apiVersion:
                    description: string. The API version to use for the model provider.
                    type: string
                  awsAccessKeyIdSecretRef:
                    description: string (Sensitive). AWS access key ID for AWS-based
                      models.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsRegionName:
                    description: string. AWS region name for AWS-based models.
                    type: string
                  awsRoleNameSecretRef:
                    description: string (Sensitive). AWS IAM role name for cross-account
                      access scenarios.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsSecretAccessKeySecretRef:
                    description: string (Sensitive). AWS secret access key for AWS-based
                      models. As with model_api_key, the value is stored in plaintext
                      in the state file; prefer a litellm_credential referenced via
                      litellm_credential_name and secure your state backend.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  awsSessionNameSecretRef:
                    description: string (Sensitive). AWS session name for cross-account
                      access scenarios.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  baseModel:
                    description: string. The actual model identifier from the provider
                      (e.g., "gpt-4", "claude-2").
                    type: string
                  customLlmProvider:
                    description: string. The LLM provider for this model (e.g., "openai",
                      "anthropic", "azure", "bedrock").
                    type: string
                  inputCostPerMillionTokens:
                    description: float. Cost per million input tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  inputCostPerPixel:
                    description: float. Cost applied per input pixel for models that
                      charge by image size.
                    type: number
                  inputCostPerSecond:
                    description: float. Cost applied per input second for audio/transcription
                      models.
                    type: number
                  litellmCredentialName:
                    description: |-
                      string. Name of a LiteLLM credential to use for this model.
                      Name of the LiteLLM credential to use
                    type: string
                  mergeReasoningContentInChoices:
                    description: boolean. When set to true, merges reasoning content
                      into the model's choices.
                    type: boolean
                  mode:
                    description: 'string. The intended use of the model. Valid values
                      are:'
                    type: string
                  modelApiBase:
                    description: string. The base URL for the model provider's API.
                    type: string
                  modelApiKeySecretRef:
                    description: string (Sensitive). The API key for the underlying
                      model provider.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  modelName:
                    description: string. The name of the model configuration used
                      to identify the model in API calls.
                    type: string
                  outputCostPerMillionTokens:
                    description: float. Cost per million output tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  outputCostPerPixel:
                    description: float. Cost applied per output pixel for image-generation
                      models.
                    type: number
                  outputCostPerSecond:
                    description: float. Cost applied per output second for audio/transcription
                      models.
                    type: number
                  pricingBaseModel:
                    description: 'string. A pricing key fed to model_info.base_model
                      independently of routing. When set, litellm_params.model still
                      routes via base_model, but LiteLLM looks up cost against this
                      key. Useful when the routing/deployment name differs from the
                      cost-map key — e.g. an Azure deployment routed as azure/gpt-4.1
                      whose real tier is Data Zone: set pricing_base_model = "us/gpt-4.1-2025-04-14"
                      so it is billed at the Data Zone rate. When unset, base_model
                      drives pricing as before.'
                    type: string
                  reasoningEffort:
                    description: 'string. Configures the model''s reasoning effort
                      level. Valid values are:'
                    type: string
                  rpm:
                    description: integer. Requests per minute limit for this model.
                    type: number
                  teamId:
                    description: string. Associate the model with a specific team.
                    type: string
                  thinkingBudgetTokens:
                    description: 'integer. Sets the token budget for the model''s
                      thinking capability. Default: 1024. Note: this field is only
                      relevant when thinking_enabled = true.'
                    type: number
                  thinkingEnabled:
                    description: 'boolean. Enables the model''s thinking capability.
                      Default: false.'
                    type: boolean
                  tier:
                    description: 'string. The usage tier for this model. Valid values
                      are "free" or "paid". Default: "free".'
                    type: string
                  tpm:
                    description: integer. Tokens per minute limit for this model.
                    type: number
                  vertexCredentialsSecretRef:
                    description: string. Vertex credentials (JSON string or path depending
                      on your setup).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  vertexLocationSecretRef:
                    description: string. Vertex AI location (e.g., us-central1).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  vertexProjectSecretRef:
                    description: string. Vertex AI project id (for custom_llm_provider
                      = "vertex").
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.baseModel is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.baseModel)
                || (has(self.initProvider) && has(self.initProvider.baseModel))'
            - message: spec.forProvider.customLlmProvider is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.customLlmProvider)
                || (has(self.initProvider) && has(self.initProvider.customLlmProvider))'
            - message: spec.forProvider.modelName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelName)
                || (has(self.initProvider) && has(self.initProvider.modelName))'
          status:
            description: ModelStatus defines the observed state of Model.
            properties:
              atProvider:
                properties:
                  additionalLitellmParams:
                    additionalProperties:
                      type: string
                    description: |-
                      map(string). A map of arbitrary additional parameters that will be merged into the litellm_params object sent to the LiteLLM API. This is intended for provider-specific or experimental options not exposed as dedicated arguments.
                      Additional parameters to pass to litellm_params beyond the standard ones
                    type: object
                    x-kubernetes-map-type: granular
                  apiVersion:
                    description: string. The API version to use for the model provider.
                    type: string
                  awsRegionName:
                    description: string. AWS region name for AWS-based models.
                    type: string
                  baseModel:
                    description: string. The actual model identifier from the provider
                      (e.g., "gpt-4", "claude-2").
                    type: string
                  customLlmProvider:
                    description: string. The LLM provider for this model (e.g., "openai",
                      "anthropic", "azure", "bedrock").
                    type: string
                  id:
                    description: The ID of the model configuration.
                    type: string
                  inputCostPerMillionTokens:
                    description: float. Cost per million input tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  inputCostPerPixel:
                    description: float. Cost applied per input pixel for models that
                      charge by image size.
                    type: number
                  inputCostPerSecond:
                    description: float. Cost applied per input second for audio/transcription
                      models.
                    type: number
                  litellmCredentialName:
                    description: |-
                      string. Name of a LiteLLM credential to use for this model.
                      Name of the LiteLLM credential to use
                    type: string
                  mergeReasoningContentInChoices:
                    description: boolean. When set to true, merges reasoning content
                      into the model's choices.
                    type: boolean
                  mode:
                    description: 'string. The intended use of the model. Valid values
                      are:'
                    type: string
                  modelApiBase:
                    description: string. The base URL for the model provider's API.
                    type: string
                  modelName:
                    description: string. The name of the model configuration used
                      to identify the model in API calls.
                    type: string
                  outputCostPerMillionTokens:
                    description: float. Cost per million output tokens. The provider
                      converts this to a per-token cost sent to the API.
                    type: number
                  outputCostPerPixel:
                    description: float. Cost applied per output pixel for image-generation
                      models.
                    type: number
                  outputCostPerSecond:
                    description: float. Cost applied per output second for audio/transcription
                      models.
                    type: number
                  pricingBaseModel:
                    description: 'string. A pricing key fed to model_info.base_model
                      independently of routing. When set, litellm_params.model still
                      routes via base_model, but LiteLLM looks up cost against this
                      key. Useful when the routing/deployment name differs from the
                      cost-map key — e.g. an Azure deployment routed as azure/gpt-4.1
                      whose real tier is Data Zone: set pricing_base_model = "us/gpt-4.1-2025-04-14"
                      so it is billed at the Data Zone rate. When unset, base_model
                      drives pricing as before.'
                    type: string
                  reasoningEffort:
                    description: 'string. Configures the model''s reasoning effort
                      level. Valid values are:'
                    type: string
                  rpm:
                    description: integer. Requests per minute limit for this model.
                    type: number
                  teamId:
                    description: string. Associate the model with a specific team.
                    type: string
                  thinkingBudgetTokens:
                    description: 'integer. Sets the token budget for the model''s
                      thinking capability. Default: 1024. Note: this field is only
                      relevant when thinking_enabled = true.'
                    type: number
                  thinkingEnabled:
                    description: 'boolean. Enables the model''s thinking capability.
                      Default: false.'
                    type: boolean
                  tier:
                    description: 'string. The usage tier for this model. Valid values
                      are "free" or "paid". Default: "free".'
                    type: string
                  tpm:
                    description: integer. Tokens per minute limit for this model.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
