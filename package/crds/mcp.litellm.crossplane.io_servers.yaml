---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: servers.mcp.litellm.crossplane.io
spec:
  group: mcp.litellm.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - litellm
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the Servers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alias:
                    description: |-
                      Alias for the MCP server. Used for easier reference.
                      Alias for the MCP server
                    type: string
                  args:
                    description: |-
                      List of arguments for the command (stdio transport only). Do not pass secrets as arguments; args are shown in plans, stored unencrypted in state, and visible in the server's process list.
                      Arguments for the command (stdio transport)
                    items:
                      type: string
                    type: array
                  authType:
                    description: |-
                      Authentication type. Valid values: none, bearer, basic. Defaults to none.
                      Authentication type (none, bearer, basic)
                    type: string
                  command:
                    description: |-
                      Command to run for stdio transport.
                      Command to run for stdio transport
                    type: string
                  description:
                    description: |-
                      Description of the MCP server.
                      Description of the MCP server
                    type: string
                  envSecretRef:
                    description: |-
                      Map of environment variables for the command (stdio transport only). Hidden from plan output but still stored unencrypted in state; secure your state backend when configuring tokens here.
                      Environment variables for the command (stdio transport)
                    properties:
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  mcpAccessGroups:
                    description: |-
                      List of access groups that can use this MCP server.
                      List of access groups for the MCP server
                    items:
                      type: string
                    type: array
                  mcpInfo:
                    description: MCP server information and configuration
                    items:
                      properties:
                        description:
                          description: |-
                            Description in MCP info.
                            Description in MCP info
                          type: string
                        logoUrl:
                          description: |-
                            Logo URL for the MCP server.
                            Logo URL for the MCP server
                          type: string
                        mcpServerCostInfo:
                          description: Cost information for MCP server tools
                          items:
                            properties:
                              defaultCostPerQuery:
                                description: |-
                                  Default cost per query for all tools.
                                  Default cost per query
                                type: number
                              toolNameToCostPerQuery:
                                additionalProperties:
                                  type: number
                                description: |-
                                  Map of specific tool names to their cost per query.
                                  Map of tool names to their cost per query
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        serverName:
                          description: |-
                            Server name in MCP info.
                            Server name in MCP info
                          type: string
                      type: object
                    type: array
                  serverName:
                    description: |-
                      Name of the MCP server.
                      Name of the MCP server
                    type: string
                  specVersion:
                    description: |-
                      MCP specification version. Defaults to 2024-11-05.
                      MCP specification version
                    type: string
                  transport:
                    description: |-
                      Transport type for the MCP server. Valid values: http, sse, stdio.
                      Transport type for the MCP server (http, sse, stdio)
                    type: string
                  url:
                    description: |-
                      URL of the MCP server. For stdio transport, use stdio:// prefix.
                      URL of the MCP server
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alias:
                    description: |-
                      Alias for the MCP server. Used for easier reference.
                      Alias for the MCP server
                    type: string
                  args:
                    description: |-
                      List of arguments for the command (stdio transport only). Do not pass secrets as arguments; args are shown in plans, stored unencrypted in state, and visible in the server's process list.
                      Arguments for the command (stdio transport)
                    items:
                      type: string
                    type: array
                  authType:
                    description: |-
                      Authentication type. Valid values: none, bearer, basic. Defaults to none.
                      Authentication type (none, bearer, basic)
                    type: string
                  command:
                    description: |-
                      Command to run for stdio transport.
                      Command to run for stdio transport
                    type: string
                  description:
                    description: |-
                      Description of the MCP server.
                      Description of the MCP server
                    type: string
                  envSecretRef:
                    additionalProperties:
                      type: string
                    type: object
                  mcpAccessGroups:
                    description: |-
                      List of access groups that can use this MCP server.
                      List of access groups for the MCP server
                    items:
                      type: string
                    type: array
                  mcpInfo:
                    description: MCP server information and configuration
                    items:
                      properties:
                        description:
                          description: |-
                            Description in MCP info.
                            Description in MCP info
                          type: string
                        logoUrl:
                          description: |-
                            Logo URL for the MCP server.
                            Logo URL for the MCP server
                          type: string
                        mcpServerCostInfo:
                          description: Cost information for MCP server tools
                          items:
                            properties:
                              defaultCostPerQuery:
                                description: |-
                                  Default cost per query for all tools.
                                  Default cost per query
                                type: number
                              toolNameToCostPerQuery:
                                additionalProperties:
                                  type: number
                                description: |-
                                  Map of specific tool names to their cost per query.
                                  Map of tool names to their cost per query
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        serverName:
                          description: |-
                            Server name in MCP info.
                            Server name in MCP info
                          type: string
                      type: object
                    type: array
                  serverName:
                    description: |-
                      Name of the MCP server.
                      Name of the MCP server
                    type: string
                  specVersion:
                    description: |-
                      MCP specification version. Defaults to 2024-11-05.
                      MCP specification version
                    type: string
                  transport:
                    description: |-
                      Transport type for the MCP server. Valid values: http, sse, stdio.
                      Transport type for the MCP server (http, sse, stdio)
                    type: string
                  url:
                    description: |-
                      URL of the MCP server. For stdio transport, use stdio:// prefix.
                      URL of the MCP server
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.serverName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serverName)
                || (has(self.initProvider) && has(self.initProvider.serverName))'
            - message: spec.forProvider.transport is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.transport)
                || (has(self.initProvider) && has(self.initProvider.transport))'
            - message: spec.forProvider.url is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.url)
                || (has(self.initProvider) && has(self.initProvider.url))'
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              atProvider:
                properties:
                  alias:
                    description: |-
                      Alias for the MCP server. Used for easier reference.
                      Alias for the MCP server
                    type: string
                  args:
                    description: |-
                      List of arguments for the command (stdio transport only). Do not pass secrets as arguments; args are shown in plans, stored unencrypted in state, and visible in the server's process list.
                      Arguments for the command (stdio transport)
                    items:
                      type: string
                    type: array
                  authType:
                    description: |-
                      Authentication type. Valid values: none, bearer, basic. Defaults to none.
                      Authentication type (none, bearer, basic)
                    type: string
                  command:
                    description: |-
                      Command to run for stdio transport.
                      Command to run for stdio transport
                    type: string
                  createdAt:
                    description: |-
                      Timestamp when the server was created.
                      Timestamp when the server was created
                    type: string
                  createdBy:
                    description: |-
                      User who created the server.
                      User who created the server
                    type: string
                  description:
                    description: |-
                      Description of the MCP server.
                      Description of the MCP server
                    type: string
                  healthCheckError:
                    description: |-
                      Error message from the last health check, if any.
                      Error message from the last health check, if any
                    type: string
                  id:
                    type: string
                  lastHealthCheck:
                    description: |-
                      Timestamp of the last health check.
                      Timestamp of the last health check
                    type: string
                  mcpAccessGroups:
                    description: |-
                      List of access groups that can use this MCP server.
                      List of access groups for the MCP server
                    items:
                      type: string
                    type: array
                  mcpInfo:
                    description: MCP server information and configuration
                    items:
                      properties:
                        description:
                          description: |-
                            Description in MCP info.
                            Description in MCP info
                          type: string
                        logoUrl:
                          description: |-
                            Logo URL for the MCP server.
                            Logo URL for the MCP server
                          type: string
                        mcpServerCostInfo:
                          description: Cost information for MCP server tools
                          items:
                            properties:
                              defaultCostPerQuery:
                                description: |-
                                  Default cost per query for all tools.
                                  Default cost per query
                                type: number
                              toolNameToCostPerQuery:
                                additionalProperties:
                                  type: number
                                description: |-
                                  Map of specific tool names to their cost per query.
                                  Map of tool names to their cost per query
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        serverName:
                          description: |-
                            Server name in MCP info.
                            Server name in MCP info
                          type: string
                      type: object
                    type: array
                  serverId:
                    description: |-
                      Unique identifier for the MCP server.
                      Unique identifier for the MCP server
                    type: string
                  serverName:
                    description: |-
                      Name of the MCP server.
                      Name of the MCP server
                    type: string
                  specVersion:
                    description: |-
                      MCP specification version. Defaults to 2024-11-05.
                      MCP specification version
                    type: string
                  status:
                    description: |-
                      Current status of the MCP server.
                      Current status of the MCP server
                    type: string
                  transport:
                    description: |-
                      Transport type for the MCP server. Valid values: http, sse, stdio.
                      Transport type for the MCP server (http, sse, stdio)
                    type: string
                  updatedAt:
                    description: |-
                      Timestamp when the server was last updated.
                      Timestamp when the server was last updated
                    type: string
                  updatedBy:
                    description: |-
                      User who last updated the server.
                      User who last updated the server
                    type: string
                  url:
                    description: |-
                      URL of the MCP server. For stdio transport, use stdio:// prefix.
                      URL of the MCP server
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
