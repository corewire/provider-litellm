---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: keys.litellm.crossplane.io
spec:
  group: litellm.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - litellm
    kind: Key
    listKind: KeyList
    plural: keys
    singular: key
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Key is the Schema for the Keys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeySpec defines the desired state of Key
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  aliases:
                    additionalProperties:
                      type: string
                    description: Map of model aliases. This allows you to create custom
                      names for models when using this key.
                    type: object
                    x-kubernetes-map-type: granular
                  allowedCacheControls:
                    description: List of allowed cache control directives. This can
                      be used to control caching behavior for requests made with this
                      key.
                    items:
                      type: string
                    type: array
                  blocked:
                    description: Whether this key is blocked. If set to true, the
                      key will be unable to make any requests.
                    type: boolean
                  budgetDuration:
                    description: Duration for the budget (e.g., "monthly", "weekly").
                      This defines the time period for which the max_budget applies.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: Configuration options for this key. This can be used
                      to set key-specific settings.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: Duration for which this key is valid. This sets an
                      expiration time for the key.
                    type: string
                  guardrails:
                    description: List of guardrails applied to this key. This can
                      be used to enforce certain safety or quality checks.
                    items:
                      type: string
                    type: array
                  keyAlias:
                    description: Alias for this key. This provides a human-readable
                      identifier for the key.
                    type: string
                  keySecretRef:
                    description: The generated API key. This is the actual key value
                      that will be used for authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  maxBudget:
                    description: Maximum budget for this key. This sets an upper limit
                      on the total spend allowed for this key.
                    type: number
                  maxParallelRequests:
                    description: Maximum number of parallel requests allowed for this
                      key. This helps in controlling concurrent usage.
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata associated with this key. This can be used
                      to store additional, custom information about the key.
                    type: object
                    x-kubernetes-map-type: granular
                  modelMaxBudget:
                    additionalProperties:
                      type: number
                    description: Maximum budget per model. This allows setting different
                      budget limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelRpmLimit:
                    additionalProperties:
                      type: number
                    description: Requests per minute limit per model. This allows
                      setting different RPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelTpmLimit:
                    additionalProperties:
                      type: number
                    description: Tokens per minute limit per model. This allows setting
                      different TPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  models:
                    description: List of models that can be used with this key. This
                      restricts the key to only use the specified models.
                    items:
                      type: string
                    type: array
                  permissions:
                    additionalProperties:
                      type: string
                    description: Permissions associated with this key. This defines
                      what actions are allowed with this key.
                    type: object
                    x-kubernetes-map-type: granular
                  rpmLimit:
                    description: Requests per minute limit for this key. This sets
                      a rate limit based on the number of API calls.
                    type: number
                  softBudget:
                    description: Soft budget limit for this key. This can be used
                      to set a warning threshold before reaching the max_budget.
                    type: number
                  tags:
                    description: List of tags associated with this key. This can be
                      used for organization and filtering of keys.
                    items:
                      type: string
                    type: array
                  teamId:
                    description: Team ID associated with this key. This links the
                      key to a specific team in the LiteLLM system.
                    type: string
                  tpmLimit:
                    description: Tokens per minute limit for this key. This sets a
                      rate limit based on the number of tokens processed.
                    type: number
                  userId:
                    description: User ID associated with this key. This links the
                      key to a specific user in the LiteLLM system.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  aliases:
                    additionalProperties:
                      type: string
                    description: Map of model aliases. This allows you to create custom
                      names for models when using this key.
                    type: object
                    x-kubernetes-map-type: granular
                  allowedCacheControls:
                    description: List of allowed cache control directives. This can
                      be used to control caching behavior for requests made with this
                      key.
                    items:
                      type: string
                    type: array
                  blocked:
                    description: Whether this key is blocked. If set to true, the
                      key will be unable to make any requests.
                    type: boolean
                  budgetDuration:
                    description: Duration for the budget (e.g., "monthly", "weekly").
                      This defines the time period for which the max_budget applies.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: Configuration options for this key. This can be used
                      to set key-specific settings.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: Duration for which this key is valid. This sets an
                      expiration time for the key.
                    type: string
                  guardrails:
                    description: List of guardrails applied to this key. This can
                      be used to enforce certain safety or quality checks.
                    items:
                      type: string
                    type: array
                  keyAlias:
                    description: Alias for this key. This provides a human-readable
                      identifier for the key.
                    type: string
                  keySecretRef:
                    description: The generated API key. This is the actual key value
                      that will be used for authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  maxBudget:
                    description: Maximum budget for this key. This sets an upper limit
                      on the total spend allowed for this key.
                    type: number
                  maxParallelRequests:
                    description: Maximum number of parallel requests allowed for this
                      key. This helps in controlling concurrent usage.
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata associated with this key. This can be used
                      to store additional, custom information about the key.
                    type: object
                    x-kubernetes-map-type: granular
                  modelMaxBudget:
                    additionalProperties:
                      type: number
                    description: Maximum budget per model. This allows setting different
                      budget limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelRpmLimit:
                    additionalProperties:
                      type: number
                    description: Requests per minute limit per model. This allows
                      setting different RPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelTpmLimit:
                    additionalProperties:
                      type: number
                    description: Tokens per minute limit per model. This allows setting
                      different TPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  models:
                    description: List of models that can be used with this key. This
                      restricts the key to only use the specified models.
                    items:
                      type: string
                    type: array
                  permissions:
                    additionalProperties:
                      type: string
                    description: Permissions associated with this key. This defines
                      what actions are allowed with this key.
                    type: object
                    x-kubernetes-map-type: granular
                  rpmLimit:
                    description: Requests per minute limit for this key. This sets
                      a rate limit based on the number of API calls.
                    type: number
                  softBudget:
                    description: Soft budget limit for this key. This can be used
                      to set a warning threshold before reaching the max_budget.
                    type: number
                  tags:
                    description: List of tags associated with this key. This can be
                      used for organization and filtering of keys.
                    items:
                      type: string
                    type: array
                  teamId:
                    description: Team ID associated with this key. This links the
                      key to a specific team in the LiteLLM system.
                    type: string
                  tpmLimit:
                    description: Tokens per minute limit for this key. This sets a
                      rate limit based on the number of tokens processed.
                    type: number
                  userId:
                    description: User ID associated with this key. This links the
                      key to a specific user in the LiteLLM system.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: KeyStatus defines the observed state of Key.
            properties:
              atProvider:
                properties:
                  aliases:
                    additionalProperties:
                      type: string
                    description: Map of model aliases. This allows you to create custom
                      names for models when using this key.
                    type: object
                    x-kubernetes-map-type: granular
                  allowedCacheControls:
                    description: List of allowed cache control directives. This can
                      be used to control caching behavior for requests made with this
                      key.
                    items:
                      type: string
                    type: array
                  blocked:
                    description: Whether this key is blocked. If set to true, the
                      key will be unable to make any requests.
                    type: boolean
                  budgetDuration:
                    description: Duration for the budget (e.g., "monthly", "weekly").
                      This defines the time period for which the max_budget applies.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: Configuration options for this key. This can be used
                      to set key-specific settings.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: Duration for which this key is valid. This sets an
                      expiration time for the key.
                    type: string
                  guardrails:
                    description: List of guardrails applied to this key. This can
                      be used to enforce certain safety or quality checks.
                    items:
                      type: string
                    type: array
                  id:
                    type: string
                  keyAlias:
                    description: Alias for this key. This provides a human-readable
                      identifier for the key.
                    type: string
                  maxBudget:
                    description: Maximum budget for this key. This sets an upper limit
                      on the total spend allowed for this key.
                    type: number
                  maxParallelRequests:
                    description: Maximum number of parallel requests allowed for this
                      key. This helps in controlling concurrent usage.
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata associated with this key. This can be used
                      to store additional, custom information about the key.
                    type: object
                    x-kubernetes-map-type: granular
                  modelMaxBudget:
                    additionalProperties:
                      type: number
                    description: Maximum budget per model. This allows setting different
                      budget limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelRpmLimit:
                    additionalProperties:
                      type: number
                    description: Requests per minute limit per model. This allows
                      setting different RPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  modelTpmLimit:
                    additionalProperties:
                      type: number
                    description: Tokens per minute limit per model. This allows setting
                      different TPM limits for each model.
                    type: object
                    x-kubernetes-map-type: granular
                  models:
                    description: List of models that can be used with this key. This
                      restricts the key to only use the specified models.
                    items:
                      type: string
                    type: array
                  permissions:
                    additionalProperties:
                      type: string
                    description: Permissions associated with this key. This defines
                      what actions are allowed with this key.
                    type: object
                    x-kubernetes-map-type: granular
                  rpmLimit:
                    description: Requests per minute limit for this key. This sets
                      a rate limit based on the number of API calls.
                    type: number
                  softBudget:
                    description: Soft budget limit for this key. This can be used
                      to set a warning threshold before reaching the max_budget.
                    type: number
                  spend:
                    description: The current spend for this key. This reflects the
                      total amount spent using this key so far.
                    type: number
                  tags:
                    description: List of tags associated with this key. This can be
                      used for organization and filtering of keys.
                    items:
                      type: string
                    type: array
                  teamId:
                    description: Team ID associated with this key. This links the
                      key to a specific team in the LiteLLM system.
                    type: string
                  tokenId:
                    type: string
                  tpmLimit:
                    description: Tokens per minute limit for this key. This sets a
                      rate limit based on the number of tokens processed.
                    type: number
                  userId:
                    description: User ID associated with this key. This links the
                      key to a specific user in the LiteLLM system.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
