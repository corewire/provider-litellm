apiVersion: litellm.crossplane.io/v1alpha1
kind: Credential
metadata:
  name: e2e-vectorstore-credential
spec:
  forProvider:
    credentialName: e2e-vectorstore-credential
    credentialInfo:
      provider: openai
    credentialValuesSecretRef:
      name: e2e-vectorstore-credential-values
      namespace: upbound-system
  providerConfigRef:
    name: default
