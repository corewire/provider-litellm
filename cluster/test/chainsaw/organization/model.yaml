apiVersion: litellm.crossplane.io/v1alpha1
kind: Model
metadata:
  name: e2e-organization-model
spec:
  forProvider:
    modelName: fake-gpt-4-organization-e2e
    customLlmProvider: openai
    baseModel: gpt-4
    mode: chat
    tier: paid
    inputCostPerMillionTokens: 30
    outputCostPerMillionTokens: 60
    modelApiKeySecretRef:
      name: e2e-organization-model-secret
      namespace: upbound-system
      key: api-key
  providerConfigRef:
    name: default
