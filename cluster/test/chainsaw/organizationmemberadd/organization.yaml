apiVersion: litellm.crossplane.io/v1alpha1
kind: Organization
metadata:
  name: e2e-organization-for-member-add
spec:
  forProvider:
    organizationAlias: e2e-org-for-member-add
    maxBudget: 100
  providerConfigRef:
    name: default
